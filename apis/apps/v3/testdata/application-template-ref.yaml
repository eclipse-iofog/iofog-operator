apiVersion: datasance.com/v3
kind: Application
metadata:
  name: health-care-wearable
  namespace: default
spec:
  template:
    name: template-name
    variables:
      - key: variable-name
        value: variable-value
